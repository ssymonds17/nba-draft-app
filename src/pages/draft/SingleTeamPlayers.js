import React from 'react';
import { useGlobalContext } from '../../context';

const SingleTeamPlayers = () => {
  const { availablePlayers } = useGlobalContext();
  return <div></div>;
};

export default SingleTeamPlayers;
